kind: "Deployment"
apiVersion: "apps/v1"
metadata:
  labels: 
    app: "samplewebapp"
  name: "samplewebapp"
spec:
  selector:
    matchLabels: 
      name: "samplewebapp"
  template: 
    metadata:
      labels:
        name: "samplewebapp"      
        app: "samplewebapp"
    spec:
      containers:
        - name: "samplewebapp"
          image: "hello-world:latest
          ports:
            - containerPort: 8080
              name: "porta1"
              protocol: TCP
  replicas: 1 
---
apiVersion: v1
kind: Service
metadata:
  name: samplewebapp
  labels:
    name: samplewebapp
    app: samplewebapp
spec:
  type: NodePort
  ports:
  - port: 8080
    name: porta2
    protocol: TCP
    nodePort: 31888
    targetPort: 8080
  selector:
    app:  samplewebapp
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: samplewebapp
  labels:
    name: samplewebapp
    app: samplewebapp
spec:
  path: /
  to:
    kind: Service
	name: samplewebapp
    weight: 100
  port:
    targetPort: 8080-tcp
  wildcardPolicy: None
